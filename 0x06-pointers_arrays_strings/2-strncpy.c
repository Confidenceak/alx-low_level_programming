#include "main.h"

/*
 * _strncpy - a function that copies a string.
 * @dest: the destination of the strin
 * @src: the sourc of the string
 * @n: the integer value
 *
 * Reeturn: 0
 */
char *_strncpy(char *dest, char *src, int n)
{
	char *ptr = dest
	{
		while (*ptr != '\0')
			*ptr++;
		do{

		}
	}
}
