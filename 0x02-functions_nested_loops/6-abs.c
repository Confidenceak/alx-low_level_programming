#include "main.h"

/**
 *  _abs - computing the absolute value of an integer
 *
 *  @r: an interger number
 *
 * Return: Absolute value
 */
int _abs(int)
{
	int r;

	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
}
