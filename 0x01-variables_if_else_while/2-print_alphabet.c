#include <stdio.h>
/**
 * main - Entry point
 * Description: Prints the alphabet in lowercase
 * Retuern: always 0 (success)
 */
int main(void)
{
	char = 97;
	int n;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
