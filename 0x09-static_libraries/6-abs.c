#include "main.h"

/**
 * _abs - this function prints absolute value of an input integer
 * @n: integer value
 *
 * Return: the absolute integer value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
