#include "main.h"

/**
 * print_triangle - printing an triangle
 *
 * @s: size of the triangle
 * @n: other sides of the triangle
 *
 * Return: Always 0.
 */
void print_triangle(int size)
{
	int s;
	int j;

	if (s <= 0)
	{
	_putchar('\n');
	{
	else (s > 0 && s <= 10, s++);
		_putchar(&#35);
	}
	while (j = s)
	{
		_putchar(&#35);
	}
	_putchar('\n');
	}
}
